<div class="container">
	<div class="row">
		<div>
    	<% if (@count.to_i) > 0 %>
      	<label class="pull-left" style="color:#336699;"> <h4>Cart Items <font color ="green">(<%= @count %>)</h4>  </font></label> 
					<table class="table table-hover">
		   			<thead>
							<div class="row">
								<tr>
                	<th>Product</th>
                  <th>Quantity</th>
                  <th class="text-center">Price</th>
                  <th class="text-center">Total</th>
								</tr>
							</thead>
              <tbody>
		         	<% total = 0 %>
							<% total_qty = 0 %>
		    			<% @carts.each do |cart| %>
								<tr>
                	<td class="col-sm-8 col-md-6">
								  	<div class="media">
						          <%= image_tag cart.get_product_image(cart.product_id) , :width => 150 %>
                      <div class="media-body">
                  	    <h4 class="media-heading"><%= cart.get_product_name(cart.product_id) %></a></h4>
                  	  </div>
										</div>
									</td>
                  <td class="col-sm-1 col-md-1" style="text-align: center">
                  	<input type="email" class="form-control" id="exampleInputEmail1" value= <%= cart.qty %>>
                  </td>
                  <td class="col-sm-1 col-md-1 text-center">
										<%= cart.get_product_price(cart.product_id) %>
									</td>
                  <td class="col-sm-1 col-md-1 text-center">
										<%= cart.get_product_price(cart.product_id) * cart.qty %>
									</td>
                  <% total = total + cart.product.price.to_i * cart.qty %> 
			  					<% total_qty = total_qty + cart.qty %> 
                  <td><%= link_to 'Destroy', cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
								</tr>
		    			<% end %>
					    <tr>
              	<td>   </td>
              	<td>   </td>
              	<td><h4><b>Total</b></h4></td>
              	<td class="text-right"><h5><strong><%= total %></strong></h5></td>
						 	</tr>
              <tr>
								<td>   </td>
                <td>   </td>
                <td>
						      <%= button_to "Continue Shopping",categories_path, :class => "btn btn-primary", :method => :get%>  
								</td>
                <td>
                	<%= button_to 'checkout', paypal_url(categories_url,@carts), :class => "btn btn-primary", :method => :post %> 
								</td> 
							</tr>
						</tbody> 
						</table>
    	<% else %>
				<label class="pull-right" style="color:#336699;"> <h4>Cart Items <font color ="red">(empty)</h4>  </font></label> 
			<% end %>	    
		</div>
	</div>
</div>

<html>
<h4 style="color:#336699;text-align:center">LIST OF CONTRIBUTORS FOR EVENTS</h4>
<hr>
<table>

  <tbody>
	<% @event_products.each do |event_product| %>
		<div class="row">
			<div class="col-sm-4 col-lg-4 col-md-4">
				<div class="thumbnail">	
					<div style="padding:4px">
					  
						For&nbsp<%= event_product.get_event_name(event_product.event_id) %>
               	<%= link_to (image_tag event_product.get_product_image(event_product.product_id), :size => "100x150"), event_product %>
            				</div>
			
					<% @percentage = 0 %>
					<% @remaining_percentage = 100 %>
					<% @contributor.get_total_contribution(event_product.id).each_pair do |key, value| %> 
	      	 			<div class="pull-right">
	            			<% @gain_price = event_product.price_to_gain %> 	
							<% @total_contribution = value %>
							<% @result = @gain_price - @total_contribution %>

							<% if @total_contribution > 0 %>
							 	<% @percentage = (@total_contribution*100) /@gain_price %>	
								<% @remaining_percentage = 100 - @percentage %>					
							<% else %>
								<% @percentage = 0 %>
								<% @remaining_percentage = 100 %>
							<% end %>
						</div>
					<% end %>
				
					<% @startdate = event_product.get_start_date(event_product.event_id) %>
					<% @enddate = event_product.get_end_date(event_product.event_id) %>

					<% days = (@enddate - @startdate).to_i %>
					
					<div class="modal-footer" style="text-align: left">
        	      		<div class="progress progress-striped active" style="background: #ddd">
        	       			<div class="bar bar-success" style="width: <%= @percentage %>%;"></div>
        	   			</div>
        				<div class="row-fluid">
        	        		<div class="span4"><b><%= @percentage %>%</b><br/><small>FUNDED</small></div>
        	        		<!--<div class="span4"><b><%= event_product.price_to_gain %>&nbspRS.</b><br/><small>PLEDGED</small></div>-->
							<div class="span4"><b><%= @remaining_percentage %>%</b><br/><small>REMAIN</small></div>
						 	<div class="span4"><b><%= days %></b><br/><small>DAYS</small></div>
							
        	      		</div>
					</div>
				</div>
			</div>
			<% if @percentage > 0 %>
			<b style="color:blue">Contributor</b> 
			<b style="color:blue">&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbspGiven Price </b> 
                                
			
			<% event_product.contributors.each do |contributor| %>
				<br>
				<%= contributor.get_user_name(contributor.user_id) %>
				&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp<%= contributor.given_price %>
			<% end %>

			<% @contributor.get_total_contribution(event_product.id).each_pair do |key, value| %> 
			<br>
	      	 <div class="span4 pull-rigth">
				<hr width="30%"/>
				<b style="color:green"><%= value %>&nbspTotal Contribution</b><br>
	            <% @gain_price = event_product.price_to_gain %> 	
				<% @total_contribution = value %>
				<% @result = @gain_price - @total_contribution %>

				<% if @result > 0 %>
					<hr width="30%"/><b style="color:red"><%= @result %>  Remaining </b> 
				<% else %>
					<hr width="30%" /> &nbsp <b style="color:green"> Achieved </b> </div>
				<% end %>
			</div>	
			<% end %>
			
			<% else %>
				<font>No DATA Available</font>
			<% end %>
				</div>
			</div>
		</div>
    <% end %>
  </tbody>
</table>
</html>

<div align="center">
	<%= will_paginate @event_products, :previous_label => 'Previous', :next_label => 'Next' %>
</div>



<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
!-->

<div class="control-group">
      <%= f.label :wish_name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :event_name, :class => 'input-xlarge' %>
      </div>
    </div>

  

<div class="control-group">
      <%= f.label :description, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :description, :class => 'input-xlarge' %>
      </div>
    </div>

  
  
   <div class="control-group">
	<%= f.label :start_date, :required => true, :class => 'control-label' %>
      <div id="datepicker" class="controls">	
		<%= f.text_field :start_date, :class => 'input-xlarge' %>
        <span class="add-on">
             <i class="icon-calendar"></i>
        </span> 				
	</div>
	</div>	   
  

  
    <div class="control-group">
		<%= f.label :end_date, :class => 'control-label' %>
      <div id="datepicker1" class="controls">
		<%= f.text_field :end_date, :class => 'input-xlarge' %>
        <span class="add-on">
						  <i class="icon-calendar"></i>
        </span> 				
			</div>	   
    </div>  
   


  <div class="actions">
     <%= f.submit 'Submit', :class => 'btn btn-primary' %>
  </div>

<div>
<%= link_to 'Back', events_path %>
</div>

<% end %>
<link rel="stylesheet" href="//code.jquery.com/ui/1.10.4/themes/smoothness/jquery-ui.css">
<script src="//code.jquery.com/ui/1.10.4/jquery-ui.js"></script>
<script type="text/javascript">
$(function() {
$('#event_start_date').datepicker({
    language: 'en',
    format: "dd/MM/yyyy"
  });


$('#event_end_date').datepicker({
    language: 'en',
    format: "dd/MM/yyyy"
  });
});

</script>
 
</div>
<%= form_for [@event, EventProduct.new], :remote => true do |f| %>
	
<br>

	<div class="row">  </div>
   <p>
	<hr>
	<!--<h4> <%= pluralize @event.event_products.size, "event_products" %> </h4>-->
	<br>
  	<div class="field">
    	<h5><b> Select Event Item </b></h5>
    	<%= select("event_product","product_id",Product.find(:all).collect {|c| [c.product_name,c.id] }, {:include_blank => '-- Select --'}, :class => "span3") %>
</p>
  	</div>

       <div class="control-group">
      		<%= f.label :price_to_gain, :class => 'control-label' %>
      			<div class="controls">
        			<%= f.number_field :price_to_gain, :required => true, :class => 'span3' %>
     			 </div>
    	</div>


  <div class="actions">
    <%= f.submit 'Submit', :class => 'btn btn-primary' %>
  </div>
   </p>

<% end %>
